# O prettier 3 já respeita o .gitignore (dist, node_modules, coverage,
# e2e-saida, *.db). Aqui fica só o que é versionado e mesmo assim não deve ser
# formatado.

# Prosa e tabela formatadas à mão. O prettier realinharia as tabelas do
# ARCHITECTURE e reembrulharia parágrafos: 177 linhas em 7 de 10 arquivos, em
# qualquer configuração, sem ganho nenhum.
*.md

# Gerado pelo npm.
package-lock.json

# Arquivos locais do Claude Code (o export do Claude Design fica aqui e não é
# versionado): o formatador não tem por que reescrever o que veio de fora.
.claude/
